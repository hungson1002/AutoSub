import type { SubtitleCue } from '../types';
import { formatClock, subtitleStats } from '../lib/subtitles';
import { Trash2 } from '../components/Icons';
export function SubtitleList({ cues, selectedId, onSelect, onChange, onDelete }: { cues: SubtitleCue[]; selectedId?: string; onSelect: (id: string) => void; onChange: (id: string, patch: Partial<SubtitleCue>) => void; onDelete: (id: string) => void }) { return <div className="subtitle-list">{cues.length === 0 ? <div className="empty-list"><span>01</span><p>Subtitle list đang trống.</p><small>Import SRT/VTT hoặc trích xuất từ video để bắt đầu.</small></div> : cues.map((cue) => { const stats = subtitleStats(cue); return <article key={cue.id} className={`cue-card ${selectedId === cue.id ? 'selected' : ''}`} onClick={() => onSelect(cue.id)}><div className="cue-meta"><span className="cue-index">#{String(cue.index).padStart(2, '0')}</span><button className="cue-delete" onClick={(event) => { event.stopPropagation(); onDelete(cue.id); }} aria-label="Xóa cue"><Trash2 size={14} /></button><span>{formatClock(cue.startMs)} → {formatClock(cue.endMs)}</span><small>{stats.duration} ms</small></div><div className="cue-row"><span>BẢN GỐC</span><textarea value={cue.originalText} onChange={(event) => onChange(cue.id, { originalText: event.target.value })} onClick={(event) => event.stopPropagation()} /></div><div className="cue-row translation-row"><span>BẢN DỊCH</span><textarea value={cue.translatedText} placeholder="Chưa có bản dịch" onChange={(event) => onChange(cue.id, { translatedText: event.target.value })} onClick={(event) => event.stopPropagation()} /></div><div className="cue-bottom"><label>START <input value={cue.startMs} type="number" onChange={(event) => onChange(cue.id, { startMs: Number(event.target.value) })} onClick={(event) => event.stopPropagation()} /></label><label>END <input value={cue.endMs} type="number" onChange={(event) => onChange(cue.id, { endMs: Number(event.target.value) })} onClick={(event) => event.stopPropagation()} /></label><div className={`cps ${stats.cps >= 20 ? 'danger' : stats.cps >= 17 ? 'warn' : ''}`}>CPS <b>{stats.cps.toFixed(1)}</b></div><div className="voice-pills">{(['G1', 'G2', 'G3'] as const).map((group) => <button key={group} className={cue.voiceGroup === group ? 'active' : ''} onClick={(event) => { event.stopPropagation(); onChange(cue.id, { voiceGroup: group }); }}>{group}</button>)}</div></div></article>; })}</div>; }
