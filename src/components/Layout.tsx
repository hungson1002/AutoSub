import type { ReactNode } from 'react';
import { AudioLines, Captions, Clapperboard, Settings2, Zap } from './Icons';
export type Page = 'translate' | 'extract' | 'editor' | 'settings';
const nav = [{ id: 'translate' as const, label: 'Dịch phụ đề', caption: 'SRT / VTT → bản dịch', icon: Captions }, { id: 'extract' as const, label: 'Trích xuất phụ đề', caption: 'OCR / âm thanh', icon: Clapperboard }, { id: 'editor' as const, label: 'Lồng tiếng video', caption: 'Dựng, style, xuất file', icon: AudioLines }, { id: 'settings' as const, label: 'Cài đặt', caption: 'Provider & hệ thống', icon: Settings2 }];
export function Layout({ page, setPage, children, cueCount, providerCount }: { page: Page; setPage: (page: Page) => void; children: ReactNode; cueCount: number; providerCount: number }) { return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark"><Zap size={16} fill="currentColor" /></div><div><strong>AutoSub</strong><span>STUDIO / LOCAL</span></div></div><div className="side-label">WORKSPACE</div><nav>{nav.map((item) => { const Icon = item.icon; return <button key={item.id} className={`nav-item ${page === item.id ? 'active' : ''}`} onClick={() => setPage(item.id)}><Icon size={18} strokeWidth={1.8} /><span><b>{item.label}</b><small>{item.caption}</small></span>{item.id === 'editor' && cueCount > 0 && <em>{cueCount}</em>}{item.id === 'settings' && providerCount > 0 && <em className="green-dot" />}</button>; })}</nav><div className="sidebar-foot"><div className="local-badge"><span className="status-dot" /> Chạy cục bộ</div><p>Không upload dữ liệu lên cloud. Provider của bạn được lưu trên máy này.</p></div></aside><main className="main-content">{children}</main></div>; }
