import type { ReactNode } from 'react';
import { X } from './Icons';
export function Modal({ open, title, eyebrow, children, onClose, wide = false, className = '', backdropClassName = '' }: { open: boolean; title: string; eyebrow?: string; children: ReactNode; onClose: () => void; wide?: boolean; className?: string; backdropClassName?: string }) { if (!open) return null; return <div className={`modal-backdrop ${backdropClassName}`.trim()} role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><div className={`modal ${wide ? 'modal-wide' : ''} ${className}`.trim()} role="dialog" aria-modal="true"><div className="modal-head"><div>{eyebrow && <div className="eyebrow">{eyebrow}</div>}<h2>{title}</h2></div><button className="icon-button" onClick={onClose} aria-label="Đóng"><X size={18} /></button></div>{children}</div></div>; }
