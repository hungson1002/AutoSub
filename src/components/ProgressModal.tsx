import { X } from './Icons';
import { Modal } from './Modal';
export function ProgressModal({ open, title, message, value, onCancel }: { open: boolean; title: string; message: string; value?: number; onCancel?: () => void }) { return <Modal open={open} title={title} eyebrow="ĐANG XỬ LÝ" onClose={onCancel || (() => undefined)}><div className="progress-copy"><span>{message}</span><strong>{typeof value === 'number' ? `${Math.round(value)}%` : '…'}</strong></div><div className="progress-track"><div style={{ width: `${value ?? 48}%` }} /></div>{onCancel && <button className="button ghost full" onClick={onCancel}><X size={15} /> Hủy</button>}</Modal>; }
